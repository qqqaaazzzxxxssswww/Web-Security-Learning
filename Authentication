========================================
PortSwigger 认证模块（Authentication）学习总结
========================================

【核心思想】
认证漏洞的本质：系统在“确认你是谁”这一步出了问题。攻击面主要有三类——
  1. 基于密码的登录（爆破 / 用户名枚举 / 防护被绕过）
  2. 多因素认证 2FA（逻辑缺陷 / 可爆破 / 可跳过）
  3. 其它认证机制（保持登录 cookie / 密码重置 / 修改密码）
记住一条主线：**只要“认证凭证”是可预测、可爆破、或校验逻辑有缺陷的，认证就能被绕过。**


==========================================================
一、基于密码的登录漏洞
==========================================================

1.1 无任何防护 —— 直接撞库爆破
--------------------------------------------------
- 场景：登录框没有速率限制、没有验证码、没有锁定。
- 做法：用 Burp Intruder，账号/密码用候选字典跑。
    · 只爆破密码 → Sniper
    · 账号+密码都爆 → Cluster bomb
- 判定命中：看响应的【状态码】或【Length（长度）】哪条与众不同；
  或用 Grep-Match 匹配 "Invalid"，没匹配到的那条即命中。

1.2 用户名枚举（Username enumeration）
--------------------------------------------------
先枚举出“哪些用户名真实存在”，再只对有效用户爆破密码，效率大增。
可利用的“差异信号”：
  · 响应内容不同：错误提示如 "Invalid username" vs "Invalid password"
  · 响应长度/状态码不同
  · 响应时间不同（Timing）：存在的用户校验密码耗时更长 → 用超长密码放大时间差
  · 行为不同：只有存在的用户会触发账户锁定
技巧：登录成功与失败的提示信息，哪怕差一个字符/一个标点，都可作为枚举依据。

1.3 防爆破机制被绕过（重点）
--------------------------------------------------
(a) 基于 IP 的封锁 → 用 X-Forwarded-For 绕过
    · 原理：服务器用来源 IP 计失败次数并封锁；但若它信任
      X-Forwarded-For 请求头来判断“客户端 IP”，就能伪造。
    · 做法：每个请求加一个不同的头：X-Forwarded-For: 1.2.3.4，
      让服务器以为每次都是新 IP，计数器永远不累积。
    · 【补充：你之前的疑问“怎么确认有没有代理服务器”】
        不需要提前确认。直接加上 X-Forwarded-For 头试即可——
        若封锁失效/计数被重置，就说明后端信任了这个头。
        这类头还有：X-Forwarded-Host、X-Originating-IP、
        X-Client-IP、True-Client-IP，可逐个试。

(b) 失败 N 次后“时间封锁”
    · 原理：同一 IP/账户连续失败若干次后，锁定几分钟。
    · 绕过思路：
        - 关键漏洞：**只要中途成功登录一次（用你自己的正确账号），
          失败计数器就会被清零**。于是构造字典时，每猜 2 次错误密码，
          就插入一次自己的正确凭证（peter），使计数永远不到锁定阈值。
        - 这就是 “Broken brute-force protection, IP block” 实验的解法。

(c) 账户锁定（Account locking）
    · 锁定策略本身会泄露“该用户名存在”→ 先枚举有效用户。
    · 部分实现只锁“连续失败”，可用多账号轮换或时间窗口绕过。

(d) 一个请求提交多组凭证
    · “Broken brute-force protection, multiple credentials per request”：
      后端按次数限制“请求数”，但一个请求体里可用 JSON 数组一次提交
      多个密码 → 一次请求试多个，绕过按请求计数的限制。

1.4 通过“修改密码”功能爆破密码（Password brute-force via password change）
--------------------------------------------------
- 核心：把“改密码”功能变成一个“当前密码对不对”的判别器。
- 改密表单字段：current-password / new-password-1 / new-password-2
- 后端逻辑差异（漏洞根源）：
    · 当前密码错          → "Current password is incorrect"
    · 当前密码对 + 新密码不一致 → "New passwords do not match"
    · 当前密码对 + 新密码一致   → 密码被真改掉（302）
- 解法：
    · username 设为 carlos；
    · **故意把两个新密码填成不同值**（如 abc / def）；
    · 对 current-password 用字典爆破。
    · 返回 "New passwords do not match" 的那条 = 当前密码猜对 = carlos 密码。
    · 因新密码不一致，carlos 密码不会被真改，很隐蔽。
- 注意点：注入点只放在 current-password；两个新密码务必不相等。


==========================================================
二、多因素认证 2FA 漏洞
==========================================================

2.1 2FA 验证码可爆破
--------------------------------------------------
- 若验证码是 4 位数字（0000-9999）且无尝试次数限制 → 直接 Intruder
  用 Numbers 载荷 0000~9999 全量爆破。
- 常配合 Burp 的 Macro/宏保持会话（每次爆破前需先过第一步登录）。

2.2 2FA 逻辑缺陷 —— 可跳过第二步
--------------------------------------------------
- 有些系统在你【输入正确账号密码后】就已经把你当成“已登录”，
  只是页面停在 2FA 步骤。此时**直接手动访问登录后页面（如 /my-account）
  就能跳过 2FA**。
- 或：验证码校验与账户绑定错误——登录 A，但在提交验证码时把
  用户名/参数改成受害者 carlos，即可用自己的验证码通过 carlos 的 2FA。

2.3 记住：2FA 不是万能
--------------------------------------------------
若第一步（账号密码）本身可绕过，或第二步与账户未正确绑定，
2FA 形同虚设。


==========================================================
三、其它认证机制漏洞
==========================================================

3.1 “保持登录” cookie 可爆破（stay-logged-in）
--------------------------------------------------
- 漏洞：cookie 由密码可预测地推导出来：
      stay-logged-in = Base64( 用户名 : MD5(密码) )
- 危害：
    · 这是一条“绕过登录框防护”的旁路认证——提交 cookie 不算“登录尝试”，
      不触发失败计数/锁定/限速。
    · 可离线构造任意用户的 cookie。
- 爆破 carlos 的解法（Burp Intruder）：
    · 注入点放在 stay-logged-in cookie 值上；
    · Payload = 密码字典；
    · Payload processing 依次：
         1) Hash → MD5
         2) Add prefix → "carlos:"
         3) Encode → Base64
    · 取消 URL-encode（否则 base64 的 "=" 被转义失效）；
    · 命中项：Status 200 / Length 异常那条 → 对应密码即 carlos 密码。
- 教训：会话/记住登录的 token 必须是**随机、服务器端存储、可撤销**的，
  绝不能由密码推导。MD5 不加盐 + 字典词 = 秒破（如 onceuponatime）。

3.2 密码重置漏洞（Reset password）
--------------------------------------------------
【token 的正常作用】
  用户忘了密码，无法用“输入原密码”证明身份。于是系统生成一个
  随机、一次性、绑定账户、会过期的 token，只发到你邮箱。
  “能拿出这个 token = 能访问该邮箱 = 你是账户主人”。
  → token 是“决定改谁密码”的唯一合法依据。

【常见漏洞】
(a) token 可预测：自增、时间戳、弱随机 → 可伪造他人重置链接。

(b) token 校验缺失 / 逻辑错误（“Password reset broken logic”实验）
    · 后端用请求里的 username 字段决定改谁密码，而不是用 token 反查。
    · 解法：拦截重置 POST，把 username=wiener 改成 username=carlos，
      token 保留原样甚至删掉，即可重置 carlos 的密码。
    · 根因：把“改谁密码”的决定权交给了用户可控字段。

(c) token 二次使用 / 不失效
    · token 用完不作废、不过期 → 可重复使用。

(d) 中间人污染 —— X-Forwarded-Host（重置链接投毒）
    · 若重置邮件里的链接域名取自 Host / X-Forwarded-Host 头，
      可将其改为攻击者域名：X-Forwarded-Host: evil.com
    · 受害者点击后，token 被发往 evil.com → 攻击者截获 token →
      拿去重置受害者密码。

3.3 修改密码逻辑漏洞
--------------------------------------------------
- 参见 1.4：改密功能可被当作爆破口，或 username 可篡改导致改他人密码。


==========================================================
四、防御要点（反过来记，帮助理解漏洞）
==========================================================
1. 登录：统一错误提示（不区分用户名/密码错）、恒定响应时间、
   基于账户+IP 的速率限制、验证码、账户锁定告警。
2. 不信任客户端可控的头（X-Forwarded-For / Host）做安全决策。
3. 会话/记住登录 token：足够随机、服务器端存储、可撤销，不由密码推导。
4. 密码存储：bcrypt / scrypt / Argon2 + 随机盐，绝不用裸 MD5/SHA1。
5. 密码重置：token 随机+一次性+绑定账户+短期过期，
   “改谁密码”只由 token 决定，绝不信任用户提交的 username。
6. 2FA：第一步与第二步严格绑定同一账户，未过 2FA 前不视为已认证，
   验证码有尝试次数限制。


==========================================================
五、Burp Intruder 速查
==========================================================
- 攻击类型：单变量=Sniper；账号+密码组合=Cluster bomb；
  同字典同步用两个位置=Pitchfork。
- 判定命中：优先看 Length / Status；辅助用 Grep-Match 匹配错误关键词。
- Payload processing：可做 Hash(MD5)、Add prefix/suffix、Base64、
  注意按需关闭 URL-encode。
- 需要登录态时：确认请求带正确 session cookie，必要时用宏维持会话。
- 免费版 Intruder 有限速，字典别过大，耐心跑。

